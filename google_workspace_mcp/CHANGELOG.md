## 0.1.2

- Fix Docker build on Alpine/Python 3.12 by installing `workspace-mcp` in a virtual environment (PEP 668 compliant).

## 0.1.1

- Bump app version metadata for Home Assistant update detection.

## 0.1.0

- Initial release of Google Workspace MCP app.
- Wraps `workspace-mcp` Python package in a Home Assistant app.
